[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --tb=short
    --strict-markers
markers =
    gpu: requires GPU
    cuda: requires CUDA
    pytorch: requires PyTorch

